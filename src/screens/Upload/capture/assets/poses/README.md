# Pose guide assets

Capture Studio overlays use **`react-native-body-highlighter`** (MIT) in ghost mode:

- Single Volt fill, `border="none"`, no muscle strokes.
- **Front** and **back** views only.

Hero frame width: `POSE_GUIDE_FRAME_WIDTH` (144px) in `PoseFigureGuide.tsx`.
